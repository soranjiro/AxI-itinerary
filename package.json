{
  "name": "axi-itinerary",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run dev:full",
    "dev:full": "pnpm run build:wasm && wrangler dev --compatibility-date 2024-09-17 --compatibility-flags nodejs_compat",
    "dev:vite": "pnpm run dev:frontend & pnpm run dev:wasm",
    "dev:open": "pnpm run dev:frontend:open & pnpm run dev:wasm",
    "build": "pnpm run build:frontend && pnpm run build:wasm",
    "deploy": "pnpm run build && wrangler deploy",
    "dev:frontend": "cd frontend && pnpm run dev",
    "dev:frontend:open": "cd frontend && rm -rf .svelte-kit && (sleep 3 && open http://localhost:5173 &) && pnpm run dev --host",
    "dev:wasm": "cd wasm && cargo build --target wasm32-unknown-unknown",
    "build:frontend": "cd frontend && pnpm run build",
    "build:wasm": "cd wasm && wasm-pack build --target web --out-dir ../frontend/src/lib/wasm"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wrangler": "^4.38.0"
  },
  "workspaces": [
    "frontend"
  ]
}
